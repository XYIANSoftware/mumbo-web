/** Horizontal centering + container used across marketing routes. */
export const PAGE_CONTAINER_CLASS = 'container mx-auto px-4'

/** Full page vertical rhythm for primary marketing pages (matches prior pattern). */
export const PAGE_SHELL_CLASS = `${PAGE_CONTAINER_CLASS} py-12`
